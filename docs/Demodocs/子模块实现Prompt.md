# Module Task: [模块编号，例如 C3] - [任务名称]

> ⚠️【不可变更声明 / Scope Lock】
> - 本模块 **必须严格、逐条对齐**《Phase1.1 开发交付包》中【[模块编号]】的全部条款。
> - **不允许**新增需求、不允许删减需求、不允许修改实现方式、不允许“等价实现 / 顺手重构 / 顺便优化”。
> - 若在实现过程中发现 **文档存在歧义、冲突或无法实现之处**，**必须立即停止**，仅返回问题清单并引用原文条款，不得自行决策。

---

## 0. Preflight（强制：先对齐条款，未完成不得写代码）

你必须先输出一张 **「Phase1.1 条款对齐表」**，逐条列出本模块在 Phase1.1 中的全部约束。

| Clause ID | Phase1.1 原文条款（保持原语义） | 你对条款的理解（1 句话，不得引入新语义） |
|----------|----------------------------------|------------------------------------------|
| C3-01 | ... | ... |

要求：
- **必须覆盖该模块全部条款**（目标 / 约束 / 真理源 / 禁止项 / 交付物 / 验收标准）。
- 若有任何条款你无法明确落地：**在此直接 STOP**，只返回问题清单，不要继续实现。

---

## 1. Context & Logic（逻辑上下文）

- **目标（What & Why）**  
  用 1 句话复述该模块目标，必须与 Phase1.1 原文语义一致，并标注 Clause ID。

- **硬约束（Hard Constraints）**
  - 每一条都必须引用 Clause ID
  - 不得弱化、合并、重新表述

- **逻辑真理源（Source of Truth）**
  - 严格按 Phase1.1 描述列出，并标注 Clause ID

- **明确不做什么（Non-Goals / 禁止项）**
  - 列出 Phase1.1 写死的禁止项（带 Clause ID）
  - 额外统一禁止：
    - ❌ 不得新增或改变 API 行为
    - ❌ 不得调整其他模块职责
    - ❌ 不得引入新锁 / 新并发模型 / 新缓存
    - ❌ 不得做“非本模块”的任何修改

---

## 2. Implementation Requirements（实现要求）

### 2.1 允许修改的代码位置（白名单）

- 在写代码前，明确列出 **将被修改或新增的文件路径清单**
- ⚠️ 除该白名单外，**不得修改任何其他文件的语义**
- 若发现必须修改白名单外文件：
  - 立即 STOP
  - 只说明原因 + 关联 Clause ID

### 2.2 核心实现步骤（逐条对齐 Clause ID）

按可审计步骤描述实现流程：
1. 做什么（Why）
2. 做什么（Why）
3. 事务 / 锁 / 状态 / 日志顺序

> 每一步都必须明确对应的 Phase1.1 Clause ID

### 2.3 事务与锁边界（如适用，必须明确）

- 锁获取位置：
- 锁内允许操作：
- 锁内禁止操作：
- 锁释放保证（异常 / finally）：

---

## 3. Verification & Evidence（工程级校验证据包 —— **唯一验收输入**）

⚠️ **你必须最终输出 1 份完整的「工程级校验证据包」文档**。  
该文档是 ChatGPT 判断“是否放行本模块”的 **唯一依据**。

### 📄 文件要求

- 文件名（固定）：
