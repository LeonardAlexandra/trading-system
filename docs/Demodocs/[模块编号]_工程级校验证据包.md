- 文件内容必须包含以下全部章节，不得缺失。

---

### 3.1 目标校验矩阵（逐条覆盖 Clause ID）

| Clause ID | Phase1.1 条款摘要 | 实现位置（文件:行号） | 校验方式（测试 / 脚本 / 命令） | 结果 |
|----------|-------------------|------------------------|---------------------------------|------|

要求：
- **Phase1.1 中该模块的每一条条款都必须有一行**
- 行号必须来自真实代码（实现完成后填写）
- 校验方式必须是可执行的（test 名 / 命令）

---

### 3.2 关键实现快照（Code Snapshot）

- 展示关键函数/方法的**完整实现**
- 必须覆盖：
- 核心逻辑
- 事务 / 锁边界
- 状态与日志写入
- 失败分支
- 每段代码注明对应 Clause ID

---

### 3.3 测试与实跑输出（原始证据）

必须粘贴 **原始命令 + 原始输出**，至少包括：

- `pytest -q`
- `pytest -ra`
- `pytest -q tests/integration`（如存在）

要求：
- 不得只给 summary
- 不得手写 PASS
- 输出必须可复现、可审计

---

### 3.4 回归与不变式声明（必须逐条回答）

在证据包中明确回答，并给出依据：

- 是否修改了既有幂等语义？（是 / 否 + 证据）
- 是否改变 ExecutionEngine / RiskManager / 事务边界？（是 / 否）
- 是否引入新的并发模型 / 缓存 / 隐式状态？（是 / 否）
- 是否存在需要人工注意的残余风险？（若有，必须说明）

---

### 3.5 变更清单（Change Manifest）

- `git diff --name-only`
- 对每个文件一句话说明：
- 为什么改
- 对应 Phase1.1 的哪条 Clause ID

---

## 4. Acceptance Criteria（放行标准）

以下 **全部满足** 才视为通过：

- [ ] Phase1.1 中本模块所有条款均在校验矩阵中逐条覆盖
- [ ] 无新增 / 删减需求，无实现方式偏移
- [ ] 行为（事务 / 锁 / 状态 / 定价等）与 Phase1.1 完全一致
- [ ] 全量 pytest 通过，无 failed / error / flaky
- [ ] 工程级校验证据包信息完整，可独立复现结论

> ❌ 任一条不满足，即视为 **不可放行**。
