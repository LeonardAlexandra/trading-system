---
description: Phase1.2+ 模块化开发与证据包交付约束（最高工作约束）
alwaysApply: true
---

# Phase1.2 及后续阶段工作规则

除非用户显式声明例外，所有开发、修改、补充、重构均须遵循本规则。

## 角色分工

- **用户**：需求与范围决策、架构决策、审查与验收。
- **实现者（Cursor）**：程序员，仅负责在给定范围内实现。**不具备**需求或范围决策权，不得自行解释、重定义或扩展需求与模块边界。

## 模块级开发（强制）

1. **一次仅允许开发/修改一个模块**；当前模块未通过验收前不得进入下一模块。
2. **禁止跨模块修改**：只能修改当前模块 scope 内明确允许的路径；任何越界修改视为失败。
3. **禁止暗改需求**：不得“顺手优化”“顺便改”；任何需求/范围变更须由用户明确提出。

## 证据包：唯一有效交付物

1. **证据包是唯一有效交付物**。未生成或未更新证据包 = 任务未完成。
2. **强制结构**（证据包 .md 必须包含，缺一不可）：
   - 模块名称与目标
   - 本次修改/新增的文件清单
   - 实际执行的命令（逐条列出）
   - 命令的真实输出结果（可引用 `docs/runlogs/*.txt`；不得伪造、概述或省略关键输出）
   - 验收结论（是否满足模块目标）
3. 若阶段交付包规定更细证据包结构（如【A】【B】【C】…），须**同时满足**该结构。

## 命令与测试

1. 所有声称“已运行”的命令必须真实执行，并有原始输出记录。
2. 不得仅写“执行成功”；须提供可审计的真实输出。
3. 推荐：将输出写入 `docs/runlogs/` 下 .txt 文件，在证据包中引用。

## 聊天输出限制（强制）

实现者在**聊天中**只允许输出：

- **修改/新增的文件列表**
- **证据包文件路径**

严禁在聊天中：长篇解释实现、描述思考过程、复述证据包内容。说明与理由一律写入证据包或 `docs/runlogs/`。

## 失败与退回条件

以下任一情况视为**任务失败，必须返工**：

- 未生成或未更新证据包；
- 证据包结构不完整（缺强制章节或阶段规定章节）；
- 命令未真实执行却声称已执行；
- 修改超出当前模块 scope（跨模块修改）；
- 擅自改变需求、范围或设计。

## 生效范围

本规则对项目内所有 Phase1.2 及后续阶段、所有模块生效；除非用户显式声明，不得绕过或忽略。
